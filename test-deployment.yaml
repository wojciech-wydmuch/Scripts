apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  replicas: 1
  containers:
    - name: nginx
      image: cizpriprocesshwtilbmefbyo2.azurecr.io/wlz-react-client:72
      volumeMounts:
      - name: basic-config
        mountPath: /etc/nginx/conf.d
  volumes:
    - name: basic-config
      configMap:
        name: basic-config
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: nginx
  name: nginx-ingress
  namespace: test
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
    appgw.ingress.kubernetes.io/use-private-ip: "true"
spec:
  rules:
  - host: nginx.cizpriprocess.local
    http:
      paths:
      - pathType: ImplementationSpecific
        path: /*
        backend:
          service:
            name: nginx
            port:
              number: 80
